name: genai-doc-assistant

channels:
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - streamlit
      - langchain
      - langchain-community
      - langchain-openai
      - google-generativeai
      - faiss-cpu
      - pypdf
      - tiktoken
      - python-dotenv